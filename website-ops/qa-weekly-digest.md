# QA-дайджест AIM Website — 2026-07-07

Источник: GitHub Actions run `28787948306` (site-qa.yml, scheduled, success),
прогон `2026-07-06T11-24-12` по production + staging + ai-native.
Локальная копия отчёта обновлена: `AIM Site Agent Evaluation/reports/latest/`.

Следующие дайджесты пишет автоматическая задача `aim-qa-weekly-digest`
(каждый понедельник 12:00, архив — в `website-ops/qa-digests/`).

## Статус

- Маршрутов: 68 · страниц: 80 · скриншотов: 51
- Findings: **76** — critical 0 · **high 47** · medium 24 · low 5
- Динамика с прошлого локального отчёта (16.06, только ai-native, 9 findings): это первый полный all-sites дайджест, сравнивать напрямую нельзя.

Разбивка high:
- **26 × HTTP 404 на production** (например `/ai-mindset-lab`, `/job-human-assistant`) — мёртвые сконфигурированные маршруты
- **9 × устаревшие даты агенды** + 1 ссылка на уже прошедшее событие
- **5 × CTA ведут не туда**, куда ожидает workflow
- **4 × большие пустые области на скриншотах** (подозрение на lazy-load)
- 1 × sitemap 404, 1 × низкое сходство контента при migration-compare

## Идеи и моменты

1. **Разобрать 404-кладбище production (26 high).** Один проход по `config/routes.production.json`: каждому мёртвому URL — либо редирект/восстановление, либо удаление из seed-списка. Это разом уберёт больше половины high findings.
2. **Даты агенды — из одного источника.** 9 страниц со старыми датами: перевести даты лаб на Sanity/общий JSON, чтобы прошедшие события снимались автоматически, а не руками.
3. **CTA-аудит перед стартом спринта III (13.07).** 5 CTA не совпадают с ожидаемым workflow — проверить связку с /pay виджетом и UTM-форвардингом, пока не пошёл трафик.
4. **Быстрый выигрыш для AI-видимости:** llms.txt, canonical, JSON-LD и markdown-зеркала отсутствуют (8 medium findings). Полдня работы — и сайт читаем агентами; заготовки уже описаны в `AIM Site Agent Evaluation/docs/05-agent-readability.md`.
5. **Пустые блоки на скриншотах** — проверить lazy-load на 4 страницах (retry-механика описана в `docs/03-visual-workflow-qa.md`); если блоки реально пустые, это баги контента.

## Требует человека

- Real-device прогоны ждут секретов BrowserStack/LambdaTest/Sauce (DeviceCloud).
- Долгая история скриншотов ждёт R2/S3 bucket (`npm run sync:history`).
- Решение по судьбе legacy-маршрутов production (восстановить vs удалить) — за владельцем.
